# python-llm 컨테이너 전용 값. backend의 .env.prod와 공유하지 않고 이 파일로 분리한다
# (DB 비밀번호 등 backend 전용 값이 이 컨테이너까지 넘어가지 않게 하기 위함).
# 실제 값 파일은 .env.python-llm으로 만들 것(gitignore됨).
# 파이썬 LLM 서버 리포의 실제 필요 환경변수 목록에 맞춰 채울 것 — 아래는 최소 추정치.

GEMINI_API_KEY=
